\section{Results}
\label{sec:results}

\subsection{The shape and growth of the field}

Quarterly output in the coded corpus rose from \firstQn{} papers in \firstQ{} to \peakQn{} in
\peakQ{} --- roughly \growthFactor{}-fold in nine quarters (Figure~\ref{fig:growth}). The
partial quarter \partialQ{} already contains \partialQn{} papers from July alone. Growth of
this kind means any narrative survey is stale on publication, which is part of why we release
the pipeline rather than only the findings.

The corpus divides into \nSystem{} system papers, \nBenchmark{} benchmarks, \nFramework{}
frameworks, \nPosition{} position papers, \nCaseStudy{} case studies, and \nSurvey{} surveys.
Its centre of gravity is computational: the largest domain is machine learning research itself,
with materials, biomedicine, and physics forming a substantial experimental minority.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\textwidth]{../figures/fig1-growth.pdf}
\caption{Papers on autonomous AI research systems per quarter of first arXiv submission
(coded corpus, $n=\nCorpus{}$). The final bar covers July 2026 only.}
\label{fig:growth}
\end{figure}

\subsection{What these systems do, and how autonomously}

Figure~\ref{fig:stages} shows lifecycle coverage and demonstrated autonomy. Literature
synthesis is the most commonly automated stage, followed by analysis and ideation; peer
review is the least. Experiment execution appears in a substantial minority, reflecting the
self-driving-laboratory subliterature.

Autonomy is more revealing. Of the \nSystemsCoded{} papers presenting a system or case study,
\nClosedLoop{} demonstrate a closed multi-stage loop with humans monitoring rather than gating,
and only \nFullAuto{} claim full autonomy from question to manuscript. The modal system
automates a single stage end to end. The field's rhetoric is dominated by end-to-end
autonomy; its demonstrated practice is not.

\begin{figure}[b]
\centering
\includegraphics[width=\textwidth]{../figures/fig2-stages-autonomy.pdf}
\caption{Left: research-lifecycle stages substantively performed (multi-select). Right:
highest autonomy level actually demonstrated, among papers presenting a system or case study.}
\label{fig:stages}
\end{figure}

\subsection{The evidentiary picture}
\label{sec:evidence}

Figure~\ref{fig:verification} is the central result. By the conventions of machine-learning
publication the corpus evaluates itself thoroughly: benchmark metrics are near-universal.
But the evaluations that would let a reader check a specific claim are rare.

\begin{itemize}\itemsep2pt
\item \pctHeldOut{} of papers re-test a selected result outside the loop that produced it.
\item \pctRealWorld{} report any external validation --- wet-lab confirmation, deployment,
reproduction of established results, or peer-review acceptance of generated work.
\item \pctHumanExpert{} involve domain experts in assessing outputs, while \pctLLMJudge{} use
LLM judges; \pctNoEval{} report no empirical evaluation at all.
\item \pctAuditNone{} --- \nAuditNone{} papers --- provide \emph{no} auditability mechanism:
no inspectable traces, no claim-to-evidence provenance, no reproduction artifacts, no
machine-checkable verification, no calibration reporting. Formal verification appears in
\nFormalVerif{} papers.
\end{itemize}

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{../figures/fig3-verification.pdf}
\caption{Evaluation methods (left) and auditability mechanisms (right) across the corpus
($n=\nCorpus{}$, multi-select). Highlighted bars mark evaluations that test a claim outside
the loop that produced it, and the share of papers offering no auditability mechanism.}
\label{fig:verification}
\end{figure}

\subsection{Validating a result and auditing a process come apart}

The prespecified operationalisation of the auditability gap --- a strong claim paired with
evaluation limited to LLM judging or nothing, and no released code --- flags \nWeakStrong{}
papers (\pctWeakStrong{}). We report that figure as specified, but it is insensitive: it
treats any benchmark metric as adequate verification, which is precisely the assumption in
question.

Conditioning on claim strength is more informative, and it separates two things the aggregate
figures blur together (Figure~\ref{fig:gap}, Table~\ref{tab:gap}). Of particular interest are
the \nDiscovery{} papers making a \emph{discovery} claim --- an assertion that the AI produced
new knowledge about the world, rather than that it performed a task well or that a method
works. These are the claims for which a reader's ability to verify matters most.

Discovery papers evaluate their \emph{results} substantially better than the corpus average,
and this half of the comparison is unambiguous. Half of them (\pctDiscRealWorld{}, CI
\ciDiscRealWorld{}) report external validation against \pctRealWorld{} corpus-wide;
\pctDiscStrongEval{} (CI \ciDiscStrongEval{}) use at least one evaluation that tests a claim
outside the loop that produced it, against \pctStrongEval{} overall; and they use held-out
transfer at three times the corpus rate. In each case the corpus rate falls far outside the
discovery interval. Authors claiming a discovery evidently feel the burden of proving the
finding, and many discharge it.

They do not discharge the other burden --- but the correct statement is weaker than the one
we first drew, and we set it out precisely because the paper's own argument is that evidence
must match the claim it is asked to support. Discovery papers provide an auditability
mechanism at \pctDiscAudit{} against \pctAnyAudit{} corpus-wide, and link a code repository at
\pctDiscRepo{} against \pctRepo{}. At $n=\nDiscPapers{}$ the intervals are \ciDiscAudit{} and
\ciDiscRepo{}, and \textbf{both contain the corpus rate}. We therefore do not claim that
discovery papers expose their process \emph{worse} than the field. What the data support is
that they expose it \emph{no better}, while validating results far better.

That asymmetry is the finding, and it is sharper than a difference would have been. The
burden authors accept and the burden they do not are visible in the same papers: proving that
the finding is real is treated as obligatory, and showing how it was reached is not. Validation
of outputs and auditability of process are different commitments, and this literature has
adopted the first without the second precisely where its claims are strongest. A reader of a
typical discovery paper can often verify \emph{that} the reported finding holds --- a material
was synthesised, a known result was reproduced --- while having no way to establish \emph{how}
the system reached it, whether the route generalises, or whether the result survived a
selection process over many discarded attempts that is nowhere reported. For a claim about
what an autonomous system can discover, the second question is the load-bearing one.

\paragraph{Robustness to how the corpus was drawn.} The review had to make contested
boundary decisions (Section~\ref{sec:method}), so we recompute the headline statistics under
three defensible definitions of the corpus: as published ($n=\nCorpus{}$); excluding every
paper admitted by the amended deep-research boundary rule, the review's most contested
inclusion decision ($n=\nNoBRSeven{}$); and restricted to papers on which both independent
screening passes agreed ($n=\nUnanimousOnly{}$).

\begin{center}\small
\begin{tabular}{lrrrr}
\toprule
corpus definition & no auditability & held-out & real-world & human role unstated \\
\midrule
as published ($n=\nCorpus{}$)          & \pctAuditNone{} & \pctHeldOut{} & \pctRealWorld{} & \pctHumanUnspec{} \\
excl.\ rule-BR7 entrants ($n=\nNoBRSeven{}$) & \pctAuditNoneNoBRSeven{} & \pctHeldOutNoBRSeven{} & \pctRealWorldNoBRSeven{} & \pctHumanUnspecNoBRSeven{} \\
unanimous screening only ($n=\nUnanimousOnly{}$) & \pctAuditNoneUnanOnly{} & \pctHeldOutUnanOnly{} & \pctRealWorldUnanOnly{} & \pctHumanUnspecUnanOnly{} \\
\bottomrule
\end{tabular}
\end{center}

No headline rate moves by more than four percentage points, and the auditability figure --- the
paper's most-cited number --- moves by under one. The corpus-boundary problem that consumed
much of this review's methodology is therefore a real difficulty for deciding \emph{which
papers} belong, and not a material source of error in \emph{what we report about them}.

\begin{table}[b]
\centering
\small
\begin{tabular}{lrrrrr}
\toprule
Headline claim & $n$ & strong eval. & real-world & any audit & code repo \\
\midrule
Discovery   & \nDiscovery{}   & \pctDiscStrongEval{} & \pctDiscRealWorld{} & \pctDiscAudit{} & \pctDiscRepo{} \\
Capability  & \nCapability{}  & \pctCapStrongEval{}  & \pctCapRealWorld{}  & \pctCapAudit{}  & \pctCapRepo{}  \\
Method      & \nMethodClaim{} & \pctMethStrongEval{} & \pctMethRealWorld{} & \pctMethAudit{} & \pctMethRepo{} \\
Conceptual  & \nConceptual{}  & \pctConcStrongEval{} & \pctConcRealWorld{} & \pctConcAudit{} & \pctConcRepo{} \\
\midrule
All papers  & \nCorpus{}      & \pctStrongEval{}     & \pctRealWorld{}     & \pctAnyAudit{}  & \pctRepo{}     \\
\bottomrule
\end{tabular}
\caption{Verification and auditability by strength of headline claim. ``Strong eval.''
means at least one of human-expert assessment, held-out transfer, or real-world validation.
Code-repository figures are verified from full text, not abstracts. For the discovery row
($n=\nDiscPapers{}$) the 95\% Wilson intervals are: strong eval.\ \ciDiscStrongEval{},
real-world \ciDiscRealWorld{}, any audit \ciDiscAudit{}, code repo \ciDiscRepo{}. The
corpus rate falls outside the first two intervals and inside the second two, which is why
we claim a difference for validation and not for auditability.}
\label{tab:gap}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.85\textwidth]{../figures/fig4-claim-vs-evidence.pdf}
\caption{Share of papers providing strong evaluation, any auditability mechanism, and a
full-text-verified code repository, by strength of headline claim.}
\label{fig:gap}
\end{figure}

\subsection{The missing human}

The dimension we did not expect to be the starkest is the role of humans.
\pctHumanUnspec{} of the corpus --- \nHumanUnspec{} papers --- either claims no human
involvement or leaves the human role unstated. Papers that name a specific human function
(gatekeeper, evaluator, or co-performer) are a minority.

The mis-description runs in both directions, which we discovered by auditing our own coding.
A consistency check found fifteen papers we had included while coding their demonstrated
autonomy as merely assistive --- a contradiction of our own inclusion rule. Adjudicating all
fifteen against full text found that eleven were \emph{under-coded rather than wrongly
included}: they present themselves as ``co-scientist'', ``human-in-the-loop'', or ``assistive''
systems while demonstrating a research stage the AI performs unaided. In one, the system
``autonomously identified and refined candidate regulators'' whose nominated target was then
validated in a physical assay, under co-scientist framing; in another, the human is explicitly
the physical actuator while the system produces the fabrication protocol. The common concern
about this literature is that it oversells autonomy. A measurable subset understates it,
describing as collaboration a process in which the AI produces the substantive output and the
human supplies the question or the hands. Both directions obstruct the same thing: a reader
trying to establish what actually happened.

This matters for interpreting every other number here. When a paper reports that a system
autonomously produced a result, the reader usually cannot tell whether a human selected the
task, chose the baseline, restarted a failed run, discarded a bad trajectory, or edited the
output. In our own project --- documented in Section~\ref{sec:reflexive} --- human
intervention changed the corpus by 183 papers, and a reader who was told only that ``an AI
conducted a systematic review'' would have no way to know that. We suspect the same is true
across much of this literature, and note that it is cheap to fix: a sentence stating what
humans did costs nothing and is currently absent from roughly seven papers in ten.

\subsection{Artifact release, measured rather than inferred}

Full-text verification of \nArtChecked{} papers finds repository links in \pctRepo{}, against
\pctRepoAbstract{} inferable from abstracts (Section~\ref{sec:artifacts}). Two points follow.
First, the field releases code more often than abstract-level reading suggests, and we correct
our own earlier estimate accordingly. Second, systematic reviews that code artifact release
from abstracts --- a common practice --- should expect a bias of this magnitude.
